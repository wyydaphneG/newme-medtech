import {getDb} from '@/lib/supabase';import {NextResponse} from 'next/server';
const esc=(v:unknown)=>`"${String(v??'').replaceAll('"','""')}"`;export async function GET(req:Request){const token=new URL(req.url).searchParams.get('token');if(!process.env.ADMIN_EXPORT_TOKEN||token!==process.env.ADMIN_EXPORT_TOKEN)return NextResponse.json({error:'Unauthorized'},{status:401});const db=getDb();if(!db)return NextResponse.json({error:'Database not configured'},{status:503});const {data,error}=await db.from('leads').select('*').order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});const cols=['created_at','name','email','whatsapp','country','company','business_type','budget','interested_product','source','message','status'];const csv=[cols.join(','),...(data||[]).map(r=>cols.map(c=>esc(r[c])).join(','))].join('\n');return new Response(csv,{headers:{'Content-Type':'text/csv','Content-Disposition':'attachment; filename="newme-leads.csv"'}})}
