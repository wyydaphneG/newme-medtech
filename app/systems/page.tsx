import type {Metadata} from 'next';import EntityCard from '@/components/EntityCard';import {getCmsSnapshot} from '@/lib/cms-repository';
export const metadata:Metadata={title:'Aesthetic Treatment Systems',description:'Explore NEWME MEDTECH systems organized around clinical applications, technologies, and treatment workflows.'};
export default async function Systems(){const cms=await getCmsSnapshot();return <><section className="soft-grid border-b border-clay/25"><div className="container-site py-16 sm:py-20"><p className="eyebrow">Clinical application structure</p><h1 className="display mt-4 max-w-4xl text-5xl sm:text-6xl lg:text-7xl">Aesthetic technology systems.</h1><p className="mt-6 max-w-2xl text-base leading-7 text-ink/70">Begin with the treatment objective, then examine the technologies and professional platforms that support it.</p></div></section><section className="container-site grid gap-5 py-20 md:grid-cols-2">{cms.systems.map(system=><EntityCard key={system.slug} href={`/systems/${system.slug}`} eyebrow="System" title={system.name} description={system.description}/>)}</section></>}
