import {notFound} from 'next/navigation';import type {Metadata} from 'next';import Link from 'next/link';import {ArrowRight} from 'lucide-react';import EntityCard from '@/components/EntityCard';import ProductCard from '@/components/ProductCard';import {getCmsSnapshot} from '@/lib/cms-repository';import {siteContact} from '@/lib/site-config';
export const dynamic='force-dynamic';export const dynamicParams=true;
export async function generateMetadata({params}:{params:Promise<{system:string}>}):Promise<Metadata>{const cms=await getCmsSnapshot();const system=cms.getSystem((await params).system);return {title:system?.seoTitle||system?.name||'Aesthetic System',description:system?.seoDescription||system?.description};}
export default async function SystemPage({params}:{params:Promise<{system:string}>}){const cms=await getCmsSnapshot();const slug=(await params).system;const system=cms.getSystem(slug);if(!system)notFound();const relations=cms.getSystemRelations(slug);return <><section className="soft-grid border-b border-clay/25"><div className="container-site py-16 sm:py-20"><p className="eyebrow">Aesthetic technology system</p><h1 className="display mt-4 max-w-4xl text-5xl sm:text-6xl lg:text-7xl">{system.name}</h1><p className="mt-6 max-w-2xl text-base leading-7 text-ink/70">{system.description}</p></div></section><section className="container-site py-20"><p className="eyebrow">Clinical applications</p><h2 className="display mt-3 text-4xl sm:text-5xl">Treatment needs within this system.</h2><div className="mt-9 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{relations.applications.map(item=><EntityCard key={item.slug} href={`/applications/${item.slug}`} eyebrow="Application" title={item.name} description={item.description}/>)}</div></section><section className="bg-sand/40 py-20"><div className="container-site"><p className="eyebrow">Technologies</p><h2 className="display mt-3 text-4xl sm:text-5xl">Mechanisms supporting the workflow.</h2><div className="mt-9 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{relations.technologies.map(item=><EntityCard key={item.slug} href={`/technologies/${item.slug}`} eyebrow={item.type} title={item.name} description={item.definition}/>)}</div></div></section><section className="container-site py-20"><p className="eyebrow">Professional platforms</p><h2 className="display mt-3 text-4xl sm:text-5xl">Products implementing these technologies.</h2><div className="mt-9 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{relations.products.map(product=><ProductCard key={product.slug} p={product}/>)}</div></section><section className="bg-sage py-14"><div className="container-site flex flex-col justify-between gap-6 sm:flex-row sm:items-center"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-charcoal/65">System consultation</p><h2 className="mt-2 font-serif text-3xl text-charcoal">Discuss applications, configurations, and market requirements.</h2></div><div className="flex flex-wrap gap-3"><Link href="/contact" className="btn bg-cream text-charcoal">Send inquiry <ArrowRight size={15}/></Link><a data-conversion="whatsapp" href={`https://wa.me/${siteContact.whatsapp}`} className="btn border border-charcoal/25 text-charcoal">WhatsApp</a></div></div></section></>}
