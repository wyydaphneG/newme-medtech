import type {Metadata} from 'next';import EntityCard from '@/components/EntityCard';import {getCmsSnapshot} from '@/lib/cms-repository';
export const metadata:Metadata={title:'Aesthetic Technologies'};
export default async function Technologies(){const cms=await getCmsSnapshot();return <><section className="soft-grid border-b border-clay/25"><div className="container-site py-16 sm:py-20"><p className="eyebrow">Physical mechanisms</p><h1 className="display mt-4 text-5xl sm:text-6xl lg:text-7xl">Aesthetic technologies.</h1><p className="mt-6 max-w-2xl text-base leading-7 text-ink/70">Understand how each technology works before comparing the platforms that implement it.</p></div></section><section className="container-site grid gap-5 py-20 md:grid-cols-2 lg:grid-cols-3">{cms.technologies.map(item=><EntityCard key={item.slug} href={`/technologies/${item.slug}`} eyebrow={item.type} title={item.name} description={item.definition}/>)}</section></>}
