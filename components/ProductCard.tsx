import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';import type {CmsProduct} from '@/lib/cms-repository';import ProductMedia from './ProductMedia';
export default function ProductCard({p}:{p:CmsProduct;i?:number}){return <Link href={`/products/${p.slug}`} className="group card overflow-hidden p-3 transition hover:-translate-y-1 hover:shadow-xl"><ProductMedia src={p.image} alt={`${p.short} professional aesthetic platform`} icon={p.icon} compact/><div className="p-4"><div className="flex items-start justify-between"><div><p className="eyebrow">{p.tag}</p><h3 className="mt-2 font-serif text-3xl">{p.short}</h3></div><ArrowUpRight className="text-sage transition group-hover:text-charcoal"/></div><p className="mt-3 min-h-12 text-sm leading-6 text-ink/65">{p.description}</p><div className="mt-5 flex flex-wrap gap-2">{p.applications.slice(0,2).map(application=><span className="rounded-full bg-sage/15 px-3 py-1 text-[10px] font-bold text-ink" key={application}>{application}</span>)}</div></div></Link>}
