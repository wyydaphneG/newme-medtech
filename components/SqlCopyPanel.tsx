'use client';
import {useState} from 'react';
export default function SqlCopyPanel({title,description,sql,step}:{title:string;description:string;sql:string;step:number}){const [copied,setCopied]=useState(false);async function copy(){await navigator.clipboard.writeText(sql);setCopied(true);setTimeout(()=>setCopied(false),2500);}return <section className="card p-7"><p className="eyebrow">Step {step}</p><h2 className="mt-3 font-serif text-3xl">{title}</h2><p className="mt-3 text-sm leading-6 text-ink/65">{description}</p><button onClick={copy} className="btn btn-primary mt-6">{copied?'已复制，可以去 Supabase 粘贴':'复制全部 SQL'}</button><details className="mt-5"><summary className="cursor-pointer text-xs font-bold text-ink/55">查看 SQL 内容</summary><textarea readOnly value={sql} className="mt-3 h-64 w-full rounded-xl border border-sage/30 bg-white p-4 font-mono text-xs"/></details></section>}
