# Product image slots

Place transparent PNG or WebP product photographs in this folder, then add the path to the matching product in `lib/products.ts`:

```ts
image: '/images/products/rf-microneedling.png'
```

Recommended: 1600 × 1600 px, centered device, transparent or cream background.
